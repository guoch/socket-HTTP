设计一个类http协议
服务器端保存一份学生名单，包括学号、照片、姓名等。名单的存放方式随意。 采用sqlite进行存储
客户端针对学生名单进行各类请求，如增加，删除，查看等，每种请求通过头部字段进行具体的要求。
增加就是
删除
查看

Socket 编程实现，推荐TCP

server sqlite query

#-*-coding:utf-8-*- 
import socket
import sys
import struct
reload(sys) 
sys.setdefaultencoding('utf-8')

address = ('localhost', 50000)
client_socket = socket.socket(socket.AF_INET, socket.SOCK_STREAM)
client_socket.connect(address)

messages = ["foobar", "barbaz", "bazquxfad", "Jimmy Carter"]

for s in messages:

    totallen = len(s) 
    pack1 = struct.pack('>I', totallen) # the first part of the message is length
    client_socket.sendall(pack1)
    client_socket.sendall(s)